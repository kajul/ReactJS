import { combineReducers} from 'redux';
import movieReducer from './MovieReducer';

const rootReducer = combineReducers({

// all reducers will be written
movieReducer

})

export default rootReducer;